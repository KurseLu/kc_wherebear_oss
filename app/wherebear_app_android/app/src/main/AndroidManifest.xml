<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 定位：對應 iOS NSLocationWhenInUse / AlwaysAndWhenInUse -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 背景定位（Android 10+）：必須「先」拿到前景定位權限，再單獨、在另一次流程中請求 -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- 前景服務＝Android 上真正能穩定背景回報的機制（對應 iOS UIBackgroundModes:location）
         Android 14 起 type=location 需要這支獨立權限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 冷啟動自動恢復回報（對應 iOS 用 UserDefaults 記住開關）：開機後把服務接回來 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 相簿匯入：讀圖 + 讀原始 EXIF 座標（對應 NSPhotoLibraryUsageDescription） -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".WhereBearApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhereBear">

        <!-- usesCleartextTraffic 只在 debug 變體開（見 src/debug/AndroidManifest.xml）：
             那是為了連本機 supabase 的 http 端點。放在這裡的話 release 會一併繼承，
             等於正式版對任意明文 HTTP 全開。對應 iOS 的 NSAllowsLocalNetworking。 -->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.WhereBear">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".location.ReportingService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <receiver
            android:name=".location.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
